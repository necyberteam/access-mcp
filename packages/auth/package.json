{
  "name": "@access-mcp/auth",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth auth service for ACCESS-CI MCP servers (CILogon proxy). Deployed as a singleton container (mcp.access-ci.org/auth), NOT an npm package \u2014 private to prevent accidental publish.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "access-mcp-auth": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "express": "^5.1.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.9",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
