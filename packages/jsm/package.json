{
  "name": "@access-mcp/jsm",
  "version": "0.2.0",
  "private": true,
  "description": "MCP server for ACCESS Jira Service Management ticket creation. Deployed as a hosted container only, NOT an npm package — files into real ACCESS JSM via a service-key-gated proxy not meant for third-party self-hosting; private to prevent accidental publish.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "access-mcp-jsm": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "access-ci",
    "jsm",
    "jira",
    "service-management",
    "support"
  ],
  "author": "ACCESS-CI",
  "license": "MIT",
  "dependencies": {
    "@access-mcp/shared": "^0.9.0",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
